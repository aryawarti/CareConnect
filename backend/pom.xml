<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.careconnect</groupId>
    <artifactId>careconnect-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>CareConnect Parent</name>
    <description>Aggregator + shared dependency management for all CareConnect services</description>

    <modules>
        <module>platform-starter</module>
        <module>config-server</module>
        <module>discovery-server</module>
        <module>api-gateway</module>
        <module>identity-service</module>
        <module>patient-service</module>
        <module>provider-service</module>
        <module>appointment-service</module>
        <module>notification-service</module>
        <module>medical-record-service</module>
        <module>billing-service</module>
        <module>queue-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-boot.version>3.4.5</spring-boot.version>
        <spring-cloud.version>2024.0.1</spring-cloud.version>
        <!-- Boot 3.4 bundles a Byte Buddy that predates JDK 25; devs run 21-25.
             Bytecode still targets 21 (maven.compiler.release). -->
        <byte-buddy.version>1.17.5</byte-buddy.version>
        <!-- Pinned explicitly. Because this project imports the Boot BOM rather
             than inheriting spring-boot-starter-parent, nothing manages plugin
             versions for us: Maven would fall back to the version bound to the
             packaging lifecycle and warn that the build is not reproducible
             ("'build.plugins.plugin.version' ... is missing"). -->
        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
        <!-- Ahead of the version Boot 3.4 manages. Docker Desktop 29 returns a
             sparse /info payload over the Windows named pipe that Testcontainers
             1.20.x rejects with a 400, so every container test silently SKIPPED
             on a machine where Docker was plainly running. Newer client, and the
             detection works again. -->
        <testcontainers.version>1.21.3</testcontainers.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Explicit entries beat imported BOMs. Property-based overrides
                 (byte-buddy.version) only work under spring-boot-starter-parent,
                 NOT with BOM imports — verified the hard way in Phase 2. -->
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy-agent</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>
            <!-- BEFORE spring-boot-dependencies on purpose: with imported BOMs
                 the first declaration wins, and a <testcontainers.version>
                 property does nothing (same trap as byte-buddy above). Declaring
                 the BOM here is the only way to move off the managed version. -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Every service gets actuator (health/metrics, NFR-5) and tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <!-- -parameters: Spring 6.1+ resolves @RequestParam/@PathVariable names
                     from compiled parameter names — no bytecode fallback anymore.
                     spring-boot-starter-parent sets this silently; our own parent
                     must do it explicitly (found via a 500 on the first @RequestParam). -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <configuration>
                        <!-- net.bytebuddy.experimental: accept JVMs newer than the
                             Byte Buddy release officially knows (e.g. JDK 25).
                             EnableDynamicAgentLoading: allow Mockito's inline agent
                             without JVM warnings on 21+. -->
                        <argLine>-Dnet.bytebuddy.experimental=true -XX:+EnableDynamicAgentLoading</argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <executions>
                        <execution>
                            <goals><goal>repackage</goal></goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
